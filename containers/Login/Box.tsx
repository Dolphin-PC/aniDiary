import { Container } from '@mui/material';
import styled from 'styled-components';

const Box = (props) => {
    return (
        <>
            <Container>
                <p>hi</p>
            </Container>
        </>
    )
}

export default Box;
